{
  "name": "node-red-contrib-mqtt-topic-selector",
  "version": "1.0.0",
  "description": "A Node-RED node for selecting topics from an MQTT broker via live-listening.",
  "author": "Andreas Böhm",
  "organization": "STFI, Chemnitz (Germany)",
    "repository": {
    "type": "git",
    "url": "https://github.com/boehand/test.git"
  },
  "license": "Apache-2.0",
  "keywords": [
    "node-red",
    "mqtt",
    "topic",
    "selector"
  ],
  "files": [
    "nodes/",
    "lib/",
    "scripts/",
    "examples/",
    "test/",
    "docs/",
    "README.md"
  ],
  "node-red": {
    "nodes": {
      "node-mqtt-topic-selector": "node-mqtt-topic-selector.js"
    }
  },
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose"
  },
  "dependencies": {
    "mqtt": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}